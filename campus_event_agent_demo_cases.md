# 校园活动与通知聚合 Agent 客户演示样例表

- 项目路径：`F:\temp\summer_work\campus_event_agent`
- 建议演示时长：12-15 分钟
- 演示数据：使用独立演示基线，结束后恢复原数据
- 判定规则：
  - 通过：HTTP 状态/页面表现符合预期，无 traceback，无编造数据，持久化/去重/幂等正确。
  - 失败：崩溃、数据污染、重复触发、错误结果、安全绕过、LLM 编造活动。
  - LLM 回复不按逐字一致判定，按“是否调用工具、是否依据真实数据、是否可重复”判定。

| # | 演示环节 | 测试输入（问题/数据） | 预期结果 | 实际结果 | 结论 |
|---|---|---|---|---|---|
| 1 | 环境自检 | `python run.py --validate`；`python -m unittest discover -s tests -p 'test_*.py'` | 数据校验无错误；现有 47 个单测全部通过 | 待执行后填写 | 待定 |
| 2 | 页面/状态 | 打开 `http://127.0.0.1:8000`；GET `/api/status` | 页面正常打开；活动数、6 个类别、LLM 状态、提醒/抓取状态可见 | 待执行后填写 | 待定 |
| 3 | 全文检索 | 左侧关键词：`马拉松` | 按演示基线返回真实活动，数量和名称与数据一致 | 待执行后填写 | 待定 |
| 4 | 类别筛选 | 左侧类别：`竞赛` | 只返回竞赛类活动，预期 5 条 | 待执行后填写 | 待定 |
| 5 | 时间筛选 | 左侧时间：`2026-11-15` | 返回 `校园志愿服务开放日` 1 条 | 待执行后填写 | 待定 |
| 6 | 自然语言时间 | Agent 问：`11.15 的活动` | 返回当天活动；标准时间 `2026-11-15T09:00:00` | 待执行后填写 | 待定 |
| 7 | 学期周边界 | Agent 问：`第3周周三 14:30 的活动` | 返回 `图书馆新生入馆培训`；标准时间 `2026-09-16T14:30:00` | 待执行后填写 | 待定 |
| 8 | 无结果 | Agent 问：`火星基地参观` | 返回 0 条；明确提示未找到；不编造活动/时间/地点 | 待执行后填写 | 待定 |
| 9 | 空/非法输入 | POST `/api/chat`：`{"message": ""}`、`{"message": "   "}`、坏 JSON；GET 未知路由；GET `/../.env` | 返回 400/500/404/403 JSON；页面不白屏；不泄露 traceback 或 .env 内容 | 待执行后填写 | 待定 |
| 10 | 提醒持久化 | Agent 问：`提醒我参加 ACM 程序设计竞赛宣讲会`；随后重启服务 | 提醒写入 `reminders.json`；状态 `pending`；重启后仍存在 | 待执行后填写 | 待定 |
| 11 | 提醒幂等/到期 | 预置一条已到期提醒；连续两次 GET `/api/reminders` | 第一次变为 `notified`；第二次不再重复触发 | 待执行后填写 | 待定 |
| 12 | 偏好与推送 | Agent 问：`我关注讲座和竞赛`；随后抓取/审核一条命中“讲座”的新活动 | 标签写入 `user_preferences.json`；新活动入库后产生 1 条未读兴趣推送 | 待执行后填写 | 待定 |
| 13 | 抓取/去重/审核 | 第一次“刷新校园活动”；第二次再刷新；批准缺名称的待审记录 | 新增 2、跳过重复 1、待审 1；第二次全部跳过；缺字段记录不能被强行入库 | 待执行后填写 | 待定 |
| 14 | LLM 降级 | 使用 `LLM_ENABLED=false` 实例，Agent 问：`下周三有什么讲座` | HTTP 200；`llm_used=false`；返回降级提示；服务不崩溃 | 待执行后填写 | 待定 |
| 15 | 抓取网络异常 | 指向无效 feed URL，且无本地示例源；Agent 问：`刷新校园活动` | 返回抓取失败信息；服务继续可用；错误写入抓取日志 | 待执行后填写 | 待定 |
| 16 | 无效日期边界 | Agent/API 输入：`11.31 的活动` | 先预演确认；若当前返回全部活动，则记为失败，由用户决定现场展示或先修复 | 待执行后填写 | 待定 |
