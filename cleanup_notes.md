# 数据收集与清洗说明

## 数据来源

`data/events.json` 当前包含 25 条手工整理活动（`EVT-*`）和 14 条南京林业大学活动预告页抓取活动（`NJFU-*`），共 39 条。手工数据按“校园活动与通知聚合 Agent”场景设计，字段参考校园通知、社团公告、教务通知和文体活动海报的常见结构。

`data/feed_candidates.json` 是定时抓取功能的本地示例源，包含 4 条记录：2 条新增、1 条重复、1 条缺名称待人工确认，用于验证新增、去重与人工确认链路。

## 清洗规则

1. 统一字段：活动名称、类别、原始时间、标准化时间、地点、来源为必填字段。
2. 时间标准化：所有活动统一写入 ISO 8601 格式 `YYYY-MM-DDTHH:MM:SS`；“第 X 周周 X”按 `2026-08-31` 为学期第一周周一的规则换算。
3. 类别归并：讲座、竞赛、社团招新、教务通知、文体活动、志愿服务等类别统一使用固定枚举，便于筛选。
4. 去重：抓取时按活动 ID 和来源 URL 去重；测试数据中 `EVT-010` 重复项会被跳过。
5. 缺失或解析失败：缺名称、缺地点或时间无法解析的记录写入 `scraping_log.jsonl` 并进入 `pending_review.json`，由人工确认后再入库。

## 样本量

- 手工整理活动（EVT-*）：25 条
- 校网抓取（NJFU-*）：14 条
- 合计：39 条
- 类别：6 类
- 时间格式：ISO 日期、`第 X 周周 X`、`X月X日`、`11.15`、`周三下午` 等
- 抓取测试源（feed_candidates.json）：4 条，其中 2 条新增、1 条重复、1 条待人工确认

