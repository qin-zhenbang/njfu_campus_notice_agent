# 复盘文档

## 1. 踩过的坑

1. 时间解析的“周”和数字容易冲突。例如 `第3周周三 14:30` 中的 `14:30` 含数字 `1`，如果按“数字代表星期几”解析会被误判为周一。最终改为只匹配 `周/星期/礼拜` 后面的中文或数字，避免时间部分干扰。
2. 抓取后容易忘记把解析结果写入活动库，导致去重失效。修复为解析成功后显式调用 `store.add_event`，再计入新增数。
3. 查询中的时间表达会残留在关键词里，导致“11.15 的活动”把 `11.15` 和 `活动` 都作为全文检索条件，结果为空。增加清洗步骤，时间范围单独提取后从查询词中移除。

## 2. 为什么这样改

- 时间解析保持规则化，方便用固定用例测试准确率。
- 抓取结果先落库再去重，保证重复检测真实可信。
- 查询词清洗与时间范围解析分离，让用户自然语言和结构化筛选互不干扰。

## 3. 如果再做一次

- 增加 Chroma 向量检索，作为关键词检索的第二路召回。
- 增加更完整的校网真实页面解析器，支持 HTML 表格和公告详情页。
- 将 JSON 存储替换为 SQLite，便于并发写入和事务管理。
- 为待办提醒增加邮件/微信等外部通知渠道。

## 4. 后续可扩展

- 真实校网爬虫与失败重试。
- 基于用户行为的个性化排序。
- 按来源、地点、标签生成统计报表。
- 前端已内置「日历 / 列表」双视图，可进一步接入日历 MCP 或导出 .ics，把活动自动写入外部日历。

